[project]
name = "xchem-fff"
version = "0.1.0"
description = "Documentation for the Fast Forward Fragments (FFF) pipeline used by XChem."
authors = [{name = "Diamond Light Source"}]
readme = "README.md"
requires-python = ">=3.12"
